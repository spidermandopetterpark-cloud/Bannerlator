name: _build (reusable matrix build)

on:
  # Permite executar manualmente em:
  # GitHub → Actions → _build → Run workflow
  workflow_dispatch:
    inputs:
      release_number:
        description: 'Label for APK file names'
        required: true
        type: string
        default: 'manual'

  # Continua permitindo que outro workflow chame este workflow
  workflow_call:
    inputs:
      release_number:
        description: 'Label for APK file names'
        required: true
        type: string

permissions:
  contents: read

env:
  ANDROID_SDK_ROOT: /usr/local/lib/android/sdk

jobs:
  build:

    runs-on: ubuntu-22.04

    strategy:
      fail-fast: false
      matrix:
        flavor:
          - { id: standard, task: assembleStandardRelease }
          - { id: ludashi,  task: assembleLudashiRelease }
          - { id: pubg,     task: assemblePubgRelease }

    name: build (${{ matrix.flavor.id }})

    steps:

    - name: Clone repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 1

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '17'

    - name: Set up Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: Resolve JavaSteam upstream commit
      id: jsref
      run: |
        SHA=$(git ls-remote \
          https://github.com/joshuatam/JavaSteam \
          gamenative-latest 2>/dev/null |
          head -1 |
          cut -f1)

        [ -z "$SHA" ] && SHA="unknown"

        echo "sha=$SHA" >> "$GITHUB_OUTPUT"
        echo "JavaSteam gamenative-latest = $SHA"

    - name: Cache JavaSteam JARs
      id: cache-javasteam
      uses: actions/cache@v4
      with:
        path: app/libs/steam
        key: javasteam-${{ steps.jsref.outputs.sha }}

    - name: Build JavaSteam and copy JARs
      if: steps.cache-javasteam.outputs.cache-hit != 'true'
      run: |
        git clone --depth 1 \
          -b gamenative-latest \
          https://github.com/joshuatam/JavaSteam \
          javasteam_src

        cd javasteam_src

        HW_FILE=$(find . \
          -name "HardwareUtils.java" \
          2>/dev/null | head -1)

        if [ -n "$HW_FILE" ]; then
          sed -i \
            's/SystemUtils\.IS_OS_ANDROID/false/g' \
            "$HW_FILE"
        fi

        UTILS_FILE=$(find . \
          -name "Utils.java" \
          -path "*/javasteam/*" \
          2>/dev/null | head -1)

        if [ -n "$UTILS_FILE" ]; then
          sed -i \
            -e 's/SystemUtils\.IS_OS_MAC_OSX_SONOMA/false/g' \
            -e 's/SystemUtils\.IS_OS_MAC_OSX_VENTURA/false/g' \
            -e 's/SystemUtils\.IS_OS_MAC_OSX_MONTEREY/false/g' \
            -e 's/SystemUtils\.IS_OS_MAC_OSX_BIG_SUR/false/g' \
            -e 's/SystemUtils\.IS_OS_WINDOWS_11/false/g' \
            "$UTILS_FILE"
        fi

        ./gradlew jar --no-daemon 2>&1 | tail -20

        MAIN_JAR=""

        while read -r j; do
          if jar tf "$j" 2>/dev/null |
             grep -q "SteamClient.class"; then
            MAIN_JAR="$j"
            break
          fi
        done < <(
          find . \
            -name "*.jar" \
            -path "*/build/libs/*" |
          grep -v "sources\|javadoc\|test" |
          sort
        )

        if [ -z "$MAIN_JAR" ]; then
          MAIN_JAR=$(
            find . \
              -name "*.jar" \
              -path "*/build/libs/*" |
            grep -v "sources\|javadoc\|test" |
            while read -r f; do
              echo "$(wc -c < "$f") $f"
            done |
            sort -rn |
            head -1 |
            cut -d' ' -f2-
          )
        fi

        DD_JAR=$(find . \
          -name "javasteam-depotdownloader-*.jar" \
          -path "*/build/libs/*" \
          ! -name "*sources*" \
          ! -name "*javadoc*" \
          2>/dev/null |
          head -1)

        mkdir -p ../app/libs/steam

        cp "$MAIN_JAR" \
          ../app/libs/steam/javasteam.jar

        PROTO_JAR=$(find ~/.gradle/caches \
          -name "protobuf-java-*.jar" \
          ! -name "*sources*" \
          ! -name "*javadoc*" \
          2>/dev/null |
          head -1)

        if [ -n "$PROTO_JAR" ]; then
          cp "$PROTO_JAR" \
            ../app/libs/steam/protobuf-java.jar
        fi

        LANG3_JAR=$(find ~/.gradle/caches \
          -name "commons-lang3-*.jar" \
          ! -name "*sources*" \
          ! -name "*javadoc*" \
          2>/dev/null |
          sort -V |
          tail -1)

        if [ -n "$LANG3_JAR" ]; then
          cp "$LANG3_JAR" \
            ../app/libs/steam/commons-lang3.jar
        else
          wget -q \
            "https://repo1.maven.org/maven2/org/apache/commons/commons-lang3/3.14.0/commons-lang3-3.14.0.jar" \
            -O ../app/libs/steam/commons-lang3.jar
        fi

        if [ -n "$DD_JAR" ] && [ -s "$DD_JAR" ]; then

          cp "$DD_JAR" \
            ../app/libs/steam/javasteam-depotdownloader.jar

          GRADLE_CACHE=~/.gradle/caches/modules-2/files-2.1

          gather_jar() {
            local pattern="$1"
            local dest="$2"
            local found

            found=$(find "$GRADLE_CACHE" \
              -name "$pattern" \
              ! -name "*sources*" \
              ! -name "*javadoc*" \
              2>/dev/null |
              sort -V |
              tail -1)

            if [ -n "$found" ]; then
              cp "$found" "$dest"
            fi
          }

          gather_jar \
            "ktor-client-cio-jvm-*.jar" \
            "../app/libs/steam/ktor-client-cio.jar"

          gather_jar \
            "ktor-client-core-jvm-*.jar" \
            "../app/libs/steam/ktor-client-core.jar"

          gather_jar \
            "ktor-io-jvm-*.jar" \
            "../app/libs/steam/ktor-io.jar"

          gather_jar \
            "ktor-http-jvm-*.jar" \
            "../app/libs/steam/ktor-http.jar"

          gather_jar \
            "ktor-utils-jvm-*.jar" \
            "../app/libs/steam/ktor-utils.jar"

          gather_jar \
            "ktor-network-jvm-*.jar" \
            "../app/libs/steam/ktor-network.jar"

          gather_jar \
            "ktor-network-tls-jvm-*.jar" \
            "../app/libs/steam/ktor-network-tls.jar"

          gather_jar \
            "kotlinx-serialization-json-jvm-*.jar" \
            "../app/libs/steam/kotlinx-serialization-json.jar"

          gather_jar \
            "kotlinx-serialization-core-jvm-*.jar" \
            "../app/libs/steam/kotlinx-serialization-core.jar"

          gather_jar \
            "kotlinx-io-core-jvm-*.jar" \
            "../app/libs/steam/kotlinx-io-core.jar"
        fi

    - name: Cache NDK + CMake
      id: cache-ndk
      uses: actions/cache@v4
      with:
        path: |
          ${{ env.ANDROID_SDK_ROOT }}/ndk/26.1.10909125
          ${{ env.ANDROID_SDK_ROOT }}/cmake/3.22.1
        key: ndk-26.1.10909125-cmake-3.22.1

    - name: Install NDK + CMake
      run: |
        SDK_ROOT="${ANDROID_SDK_ROOT}"

        mkdir -p "$SDK_ROOT"

        if [ ! -f "$SDK_ROOT/ndk/26.1.10909125/source.properties" ]; then

          if [ ! -f "$SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" ]; then

            curl -sL \
              "https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip" \
              -o /tmp/cmdline-tools.zip

            unzip -q \
              /tmp/cmdline-tools.zip \
              -d /tmp/cmdline-tools/

            mkdir -p "$SDK_ROOT/cmdline-tools"

            mv \
              /tmp/cmdline-tools/cmdline-tools \
              "$SDK_ROOT/cmdline-tools/latest" \
              2>/dev/null || true

            rm -f /tmp/cmdline-tools.zip
          fi

          yes | "$SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" \
            --install \
            "ndk;26.1.10909125" \
            "cmake;3.22.1"

        else
          echo "NDK cache hit"
        fi

        echo \
          "ANDROID_NDK_HOME=$SDK_ROOT/ndk/26.1.10909125" \
          >> "$GITHUB_ENV"

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Grant execute permission for LSFG script
      run: chmod +x build-lsfg-android.sh

    - name: Resolve pinned imagefs hash
      id: imagefs-pin
      run: |
        SHA=$(grep -oP \
          'imageFSSha256\s*=\s*"\K[0-9a-f]{64}' \
          app/build.gradle)

        if [ -z "$SHA" ]; then
          echo "Could not read imageFSSha256"
          exit 1
        fi

        echo "sha=$SHA" >> "$GITHUB_OUTPUT"

    - name: Cache base imagefs
      uses: actions/cache@v4
      with:
        path: app/src/main/assets/imagefs.tar.zst
        key: imagefs-${{ steps.imagefs-pin.outputs.sha }}

    - name: Cache LSFG native library
      id: cache-lsfg
      uses: actions/cache@v4
      with:
        path: app/src/main/jniLibs/arm64-v8a/libVkLayer_LSFGVK_frame_generation.so
        key: lsfg-${{ hashFiles('app/src/main/cpp/lsfg-vk/**', 'build-lsfg-android.sh') }}

    - name: Build LSFG native library
      if: steps.cache-lsfg.outputs.cache-hit != 'true'
      run: ./build-lsfg-android.sh

    - name: Build ${{ matrix.flavor.id }}
      run: |
        ./gradlew \
          ${{ matrix.flavor.task }} \
          -Dorg.gradle.jvmargs="-Xmx5g -XX:MaxMetaspaceSize=1g"

    - name: Get version info
      run: |
        echo "SHORT_SHA=$(git rev-parse --short HEAD)" >> "$GITHUB_ENV"
        echo "VERSION=$(grep versionName app/build.gradle | head -1 | cut -d '"' -f 2)" >> "$GITHUB_ENV"
        echo "BUILD_DATE=$(date +'%Y%m%d')" >> "$GITHUB_ENV"

    - name: Rename APK
      run: |
        N="${{ inputs.release_number }}"
        F="${{ matrix.flavor.id }}"

        mv \
          "app/build/outputs/apk/${F}/release/app-${F}-release.apk" \
          "app/build/outputs/apk/${F}/release/Bannerlator-${N}-${F}.apk"

    - name: Upload ${{ matrix.flavor.id }} APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: Bannerlator-${{ inputs.release_number }}-${{ matrix.flavor.id }}
        path: app/build/outputs/apk/${{ matrix.flavor.id }}/release/Bannerlator-${{ inputs.release_number }}-${{ matrix.flavor.id }}.apk
        if-no-files-found: error
        retention-days: 7
